# Dispatcher Prompt - R22 Materialized Feature Strategy Lab

Batch: `WINDOWS_WSL2_MATERIALIZED_FEATURE_STRATEGY_LAB_R22_20260708`

Read:

- `tasks/in_progress/windows-wsl2-materialized-feature-strategy-lab-r22-20260708/spec.md`
- C1 result summary and closeout.
- A_Share_Monitor R21/C1 materialized feature artifacts.
- strategy_work C1 final sync.

## Execute

Run R22 as an ordinary research-only strategy diagnostics batch after R21/C1.

Before diagnostics:

1. Freeze C1/R21 materialized evidence.
2. Import C1/R21 outputs into experiment store.
3. Update failure memory.
4. Confirm source-health for any source-heavy task.

## Objective

Use the newly materialized ETF amount/turnover rows and pass77 A-share feature rows for focused limitation-aware diagnostics.

This is not source-review and not rule cleanup. Do not revisit controller/gate cleanup unless a concrete blocker appears.

## Priority

- ETF: use 32,482 amount/turnover rows for liquidity-aware and turnover-throttled diagnostics. NAV/premium remains partial/unresolved unless public evidence improves.
- A-share: use 136,767 pass77 feature rows and five fixed features to explain validation/test divergence.
- Global/news/macro: use 100 context rows and 13-symbol global regime evidence only as context and divergence attribution.
- Strategy probes: only run conditional probes after prequalification. Full-frame wide3068 remains forbidden.

## Boundary

Research-only. No actionable output, candidate promotion, readiness promotion, route activation, daily signal push, raw-data migration into controller, active schema/registry activation, or credential output.

## Callback

Return the callback envelope required in the task packet, including materialized data status, diagnostics status, eligible count, candidate availability, boundary result, and next source action.
