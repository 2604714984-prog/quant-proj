# Dispatcher Prompt - R30 Strategy Factory

Batch: `WINDOWS_WSL2_STRATEGY_FACTORY_R30_20260709`

Read:

- `tasks/in_progress/windows-wsl2-strategy-factory-r30-20260709/spec.md`
- latest R29 result if available.
- R28/R29 direct market-cap artifacts.
- SmallCap R26/R27/R28 evidence packages.
- R25/R26/R27/R28 audit results.

## Execute

Run R30 as one large research-only strategy factory batch.

## Objective

Continue inside one controlled batch until it reaches one of these outcomes:

1. At least one `LOCAL_RESEARCH_PROBE_ELIGIBLE` research line.
2. All active strategy surfaces are exhausted and labelled observation/retired/do-not-retry under current evidence.
3. A hard source/auth boundary requires separate user authorization.

## Priority order

1. Finish R29 direct market-cap membership gate for SmallCap if not already closed.
2. If direct market-cap evidence is available, run the decisive SmallCap leakage/timing and robustness path.
3. If SmallCap is blocked, harden US30W-R22-002 preservation and robustness.
4. If pass77 direct/proxy evidence changes, run repaired diagnostics; otherwise keep gated.
5. Do not reopen retired ETF rotation unless new accepted evidence changes its instability premise.
6. Only after active surfaces are blocked, run pre-registered new strategy families.

## Hard rule

Do not stop after producing another process report. The batch must end with a final strategy factory board:

- `LOCAL_RESEARCH_PROBE_ELIGIBLE`, or
- `NO_VERIFIABLE_STRATEGY_UNDER_CURRENT_EVIDENCE`, or
- `BLOCKED_BY_AUTH_OR_SOURCE_LIMIT`.

## Boundary

Research-only. No actionable output, candidate promotion, readiness/product route, daily signal, broker/order/paper/live/auto path, active registry/schema change, credential output, full-frame wide3068, or test-result parameter selection.

## Callback

Return callback with active surface status, strategy family results, final board counts, local research probe eligible count, strategy candidate availability, boundary result, fixes required, and next source action.
