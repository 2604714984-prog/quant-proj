# Current delta

- Resume the preserved isolated worktree at
  `/home/rongyu/workspace/.wt-baostock-fundamental-smoke-20260710`.
- Only the materializer script, its focused test, and the runops schema are in
  scope.
- The recovered draft now passes 12 focused tests before commit.
- Preserve `gpMargin=query_profit_data` and the frozen Family 68 fields:
  `pubDate`, `statDate`, `roeAvg`, `gpMargin`, `CFOToNP`,
  `liabilityToAsset`, and `YOYAsset`.
- No provider call, `--execute-network`, canonical DB/cache/data write,
  migration, activation/readiness, strategy evaluation/promotion,
  recommendation, broker/order/paper/live/auto, or secret access/output.
- Final implementation callback goes to Quant-Dispatcher task
  `019f4ca0-2054-77e3-9559-7005c0f9b565`.
