# Human-Gate

This packet authorizes code-only implementation, mock/tmp-path tests, Git
commit, and Git push. It does not authorize provider login/network execution,
`--execute-network`, canonical DB/cache/data writes, migration, activation, or
any trading/recommendation path. Future provider smoke execution requires a
separate durable task-level HG record matching the implementation commit.
