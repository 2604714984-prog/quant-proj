# Human-Gate - TASK-007

Required: yes

Available record:

- `HG-NIGHT-BATCH-20260704-L1-L4`

Permission levels:

- `L1_CONTROLLED_DB_WRITE`
- `L2_CONTROLLED_NETWORK_INGEST` only if a remote provider is used

Execution requirements:

- command transcript;
- `--allow-write`;
- `--allow-network` if remote provider;
- snapshot id;
- manifest/counts/hashes;
- Codex acceptance.

Non-authorization:

This record does not authorize active registry replacement, recommendation readiness, HITL ticket emission, broker/order paths, paper/live trading, auto execution, trade plans, entry prices, target weights, position sizing, allocation, `.env` reads, or key output.
